# Coupang (酷澎) 報價單欄位對應與資料格式規範手冊

> 本文件詳細定義來源 Excel 資料表在轉換為酷澎 (Coupang) 標準報價單時的**欄位對照關係**、**格式規範**、**自動轉換規則**與**資料驗證檢查機制**。

---

## 1. 來源檔案基本結構設定

| 設定項目 | 預設值 | 說明 |
| :--- | :--- | :--- |
| **工作表名稱 (Sheet Name)** | `Sheet1` | 支援任意工作表名稱（如 `Sheet1`、`工作表1`、`商品資料` 等），亦可於對照表設定中自訂。 |
| **標題列 (Header Row)** | 第 `3` 列 | 包含各欄位名稱之列號。 |
| **資料起始列 (Row Start)** | 第 `4` 列 | 實際商品資料開始之列號（支援在介面上自由設定起始與結束列）。 |
| **篩選欄位 (Filter Column)** | `中文背標` | 僅處理此欄位有填寫內容之列（若第 1 欄背景色為淺粉紅 `#FAD9D6` 亦會自動略過）。 |

---

## 2. 來源與目標欄位對照及格式規範表

| 來源檔案欄位名稱 | 對應目標報價單欄位 | 必要性 | 格式規範與範例 | 系統內部處理與轉換規則 |
| :--- | :--- | :--- | :--- | :--- |
| **`中文品名`**<br>*(或 商品名稱 / 品名)* | 1. `商品名稱`<br>2. `細分商品種類`<br>3. `品牌`<br>4. `系列`<br>5. `顏色`<br>6. `尺寸` | **必填**<br>(最核心) | **結構化字串**<br>結構：`[品牌] [系列名] [品類] [顏色] [規格/尺寸]`<br>範例：<br>• `品牌名 隱士系列 頂級皮革狗項圈 經典黑 L`<br>• `品牌名 聖特羅佩系列 雙層皮革X型犬用胸背帶 經典黑 35-1.0` | 系統核心剖析器會自動拆解：<br>1. **品類判定**：依關鍵字判定使用模板與分類代碼（胸背帶 `66030`、牽繩 `66027`、項圈 `66025`）。<br>2. **屬性萃取**：萃取品牌、系列、中文顏色與尺寸填入對應欄位，並用於比對照片資料夾。 |
| **`中文背標`**<br>*(或設定之篩選欄位)* | *(無，僅作為篩選條件)* | **必填**<br>(篩選依據) | **字串 / 標記**<br>範例：任意文字內容（非空即納入處理）。 | 判斷此 SKU 是否需要產生報價單。 |
| **`EAN`**<br>*(或 商品條碼 / 條碼 / SKU)* | **`商品條碼`** | **必填** | **純數字 / 文字字串**<br>範例：`8051234567890`<br>⚠️ 禁止科學記號 (如 `8.05E+12`) | 系統會自動格式化去除小數點 `.0`，並在目標 Excel 設定為文字格式 `@`，避免條碼變形或失真。 |
| **`酷澎進價`** | **`酷澎進價 (含稅)`** | **必填** | **正數數值 / 金額**<br>範例：`1200` 或 `850.5`<br>⚠️ 不得含幣別符號 (如 `$`, `NT`) | 填入目標模板作為酷澎採購進價。 |
| **`台灣訂價`** | **`建議酷澎售價`** | **必填** | **正數數值 / 金額**<br>範例：`2400` | 填入目標模板作為商品建議售價。 |
| **`每單位包裝尺寸(mm)`**<br>*(或 `每單位包裝尺寸\n(mm)`)* | **`每單位包裝尺寸(mm)`** | **建議填寫** | **長\*寬\*高 格式**<br>範例：`120*80*25` 或 `12*8*2.5cm` | 系統會自動清理字串（將 `x`、`×` 統一轉為 `*`），若包含 `cm` 則會自動乘以 10 轉為整數 `mm`，標準輸出如 `120*80*25`。 |
| **`每單位包裝重量(g)`**<br>*(或 `每單位包裝重量\n(g)`)* | **`每單位包裝重量(g)`** | **建議填寫** | **整數數值 (公克)**<br>範例：`150` | 系統會自動四捨五入轉換為整數公克值。 |
| **`COLLECTION`**<br>*(或 系列 / Collection)* | *(輔助比對)* | 選填<br>(輔助) | **英文系列名**<br>範例：`HERMITAGE`, `AMALFI`, `MONZA` | 用於照片目錄搜尋及自動中英文系列字典對照。 |
| **`TYPE`**<br>*(或 種類 / 品類 / Type)* | *(輔助比對)* | 選填<br>(輔助) | **英文品類名**<br>範例：`DOG COLLAR`, `X HARNESS`, `LEASH` | 用於輔助分類與判斷目標模板。 |
| **`COLOR`**<br>*(或 顏色 / Color)* | *(輔助比對)* | 選填<br>(輔助) | **英文顏色名**<br>範例：`BLACK`, `BROWN & TURQUOISE` | 用於照片目錄搜尋及自動中英文顏色字典對照。 |
| **`SIZE`**<br>*(或 尺寸 / Size)* | *(輔助比對)* | 選填<br>(輔助) | **尺寸規格代碼**<br>範例：`L`, `M`, `110-1.2`, `35-1.0` | 當中文品名結尾未標註尺寸時之備用，亦用於背標檔名與照片配對。 |

---

## 3. 品類判定與 Excel 模板/分類代碼自訂對應 (Category Rules)

系統支援**使用者完全自訂品類規則**，藉由來源檔案的 `TYPE` 欄位或 `中文品名` 關鍵字，動態決定套用的 Excel 模板種類、酷澎細分分類代碼及輸出子資料夾：

| 品類名稱 | 匹配關鍵字 (TYPE / 品名) | 套用 Excel 模板 | 酷澎細分商品種類 (分類代碼及名稱) | 預設輸出子資料夾 |
| :--- | :--- | :--- | :--- | :--- |
| **胸背帶** | `HARNESS`, `胸背帶`, `背帶` | 胸背帶模板 (`商品報價單_胸背帶.xlsx`) | `寵物用品>狗用品>牽繩/胸背帶>胸背帶 (66030)` | `胸背帶` |
| **牽繩** | `LEASH`, `牽繩` | 項圈牽繩模板 (`商品報價單_項圈 牽繩.xlsx`) | `寵物用品>犬貓通用>項圈/伸縮牽繩>牽繩 (66027)` | `項圈牽繩` |
| **項圈** | `COLLAR`, `項圈` | 項圈牽繩模板 (`商品報價單_項圈 牽繩.xlsx`) | `寵物用品>犬貓通用>項圈/伸縮牽繩>項圈 (66025)` | `項圈牽繩` |
| *(自訂品類)* | *(可自訂任意字串，如 TAG, 衣服)* | 依自訂指定 | 依酷澎官方規範自訂 (包含分類代碼) | 依自訂目錄名稱輸出 |

> 💡 **擴充提示**：使用者可隨時在「對照表設定 ➔ 品類與分類代碼設定」中新增新品類規則，或調整既有規則的分類代碼與套用模板。

---

## 4. 目標報價單固定填寫欄位 (Fixed Mappings)

以下欄位由系統依設定自動填寫，來源 Excel 毋需提供：

| 目標報價單欄位名稱 | 固定填寫內容 | 說明 |
| :--- | :--- | :--- |
| **細分商品種類** | 由品類判定規則自動填入對應代碼 | 依品類規則自動分流 |
| **品牌** | 自動由中文品名解析或於設定中自訂 | 品牌資訊（品名含品牌時自動萃取填入） |
| **數量** | `1個` | 包裝件數 |
| **是否應稅** | `應稅(5%)` | 稅別 |
| **製造廠商** | 自動依品牌填入或於設定中自訂 | 製造商資訊 |
| **供貨方式** | `官方代理商` | 供貨商身份 |
| **是否為進口商品** | `進口商品` | 進口標記 |
| **單箱商品入數** | `1` | 箱入數 |
| **商品保存天數** | `1095` | 保存效期 (天) |
| **是否為易碎品** | `不適用` | 易碎標記 |
| **國內製造商或負責商名稱** | `緯豪實業有限公司` | 代理商公司名 |
| **原產地/國** | `中國` | 產地資訊 |
| **注意事項/備註欄** | `請參考產品實際包裝所示` | 備註說明 |
| **包裝上方 (內部上架審核用)** | `嚴正聲明-本商品無外包裝照片` | 內部審核聲明 |
| **包裝下方 (內部上架審核用)** | `嚴正聲明-本商品無外包裝照片` | 內部審核聲明 |
| **包裝左側 (內部上架審核用)** | `嚴正聲明-本商品無外包裝照片` | 內部審核聲明 |
| **包裝右側 (內部上架審核用)** | `嚴正聲明-本商品無外包裝照片` | 內部審核聲明 |

---

## 5. 圖片與背標命名與關聯規則

| 圖片類型 | 目標檔案欄位 | 匯出檔案命名格式 | 規範與自動處理 |
| :--- | :--- | :--- | :--- |
| **商品主圖** | `商品正面(主要圖片）` | `{去除尺寸之中文品名}主圖.jpg` | 自動檢查寬度，小於 500px 自動等比放大。同品名同顏色之多尺寸商品共用同一張主圖。 |
| **情境圖 1** | `商品側拍或情境圖 1\n(消費者可見圖片）` | `{編號}.jpg` (如 `1.jpg`) | 自動檢查寬度與等比縮放，同系列款式共用編號。 |
| **情境圖 2** | `商品側拍或情境圖 2\n(消費者可見圖片）` | `{編號}.jpg` (如 `2.jpg`) | 自動檢查寬度與等比縮放。 |
| **尺寸規格表** | `詳細說明圖集` (或尺寸圖相關欄位) | `尺寸規格表_{去除尺寸之中文品名}.jpg` | 若該顏色資料夾無尺寸表，自動向上層系列資料夾遞迴搜尋。 |
| **商品背標圖** | 包含「背標」字樣之欄位 | `背標_{完整中文品名}_{尺寸}.jpg` | 依據 SKU 尺寸規格精確匹配對應背標檔。 |

---

## 6. 資料檢查與驗證機制規範 (Validation Rules)

為確保報價單產出之品質與符合酷澎上架標準，系統執行以下檢查機制：

1. **表頭與必要欄位完整性檢查**：
   - 來源工作表必須包含 `中文品名`、`中文背標`、`EAN`、`酷澎進價`、`台灣訂價`。
   - 若缺少必要欄位，應立即中斷並提示使用者缺少哪些欄位。
2. **中文品名結構合法性**：
   - 中文品名字串不得為空，且必須能拆解出有效品類（胸背帶、牽繩、項圈）。
   - 若品名格式無法解析，應於介面日誌發出黃色警告並將目標儲存格標註警示底色。
3. **條碼格式合法性**：
   - 條碼值不得為空，且不得呈現科學記號（如 `8.05E+12`）。
   - 必須確保輸出為純文字格式，避免前導零消失或被 Excel 自動轉換。
4. **價格邏輯合理性**：
   - `酷澎進價` 與 `建議酷澎售價` 必須為大於 0 之合法數值。
   - 進價不得高於售價（若進價 > 售價則發出警示）。
5. **包裝尺寸與重量格式**：
   - 包裝尺寸若有填寫，必須符合 `長*寬*高` 之三維數值格式。
   - 包裝重量若有填寫，必須能轉換為有效之公克整數。
6. **圖片配對合規性檢測**：
   - 檢查每筆商品是否皆成功配對到「主要圖片」；若缺圖則計入缺圖統計並在日誌標記 SKU 與品名。
