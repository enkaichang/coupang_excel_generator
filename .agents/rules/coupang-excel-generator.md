---
trigger: always_on
---

每次新增/修正完內容依修改大小分成：大改、中改、小改。
版本號 a.b.c
大改 a增加一 中改 b增加一 小改 c增加一
新增該新版本號的版本日誌，只寫重點，不須寫一些之微末節的小細節
更新後要去README.md更新版本號即日誌
所有檔案的版本號要同步

本專案原則是"寧可空白，也不要抓錯"，抓取資料最多設一個fallback，若還是抓不到則留空

icon都採用material icon不使用emoji

絕不重覆寫相同的邏輯，若有與其他段程式相同的邏輯則建立一個函數共用。僅可能減少「屎山代碼」的產生。

絕對不能為了解決當前問題而在程式寫死當前的情況，例如當前在實作`芬蘭 Rukka`，不可以在程式裡面植入`芬蘭 Rukka`來解決用戶提出的問題，這樣會喪失此專案的廠商通用性。